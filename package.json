{
  "name": "nebula-mail",
  "version": "1.0.0",
  "private": true,
  "description": "Nebula Mail — AI-Powered Mail Web Application with Live Gmail Sync",
  "scripts": {
    "test": "node backend/tests/test_suite.js",
    "start:backend": "node backend/server.js",
    "build:frontend": "npm run build --prefix frontend",
    "dev:frontend": "npm run dev --prefix frontend"
  },
  "dependencies": {
    "better-sqlite3": "^13.0.3"
  }
}
